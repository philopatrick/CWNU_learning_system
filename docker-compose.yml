version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: cwnu-learning-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: cwnu_learning_system
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
